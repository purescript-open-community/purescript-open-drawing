{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "example" : "spago -x ./example.dhall bundle-app --main Main --to html/index.js"
  },
  "devDependencies": {
    "pulp": "^16.0.0",
    "purescript": "^0.15.0",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2",
    "spago": "^0.20.9"
  }
}
